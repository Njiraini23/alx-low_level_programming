#include "main.h"
/**
 *_abs - The abs computes absolute value of an integer
 * @n: the integer to be returned
 *Return: the abslute value of an int
 */
int _abs(int n)
{
	if (n >= 0)
		return (n);
	else
		return (-n);
}
